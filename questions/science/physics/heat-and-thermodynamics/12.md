---
choices:
  - $Q, T, W$
  - $Q, W$
  - $P, T, Q$
answers:
  - $P, T, U$
---

**Identify the state variables given here:**

```markdown
State variables are properties that depend only on the state of the system, not on the path taken. Pressure (P), Temperature (T), and Internal Energy (U) are state variables. Heat (Q) and Work (W) are path functions, not state variables.
```