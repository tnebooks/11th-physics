---
choices:
  - $\hat{i} + \hat{k}$
  - $\hat{i} - \hat{j}$
  - $\hat{k} - \hat{j}$
answers:
  - $\hat{i} + \hat{j}$
---

**Identify the unit vector in the following.**

```markdown
A unit vector has a magnitude of 1. For a vector to be a unit vector, the sum of squares of its components must equal 1. Among the given options, $\hat{i} + \hat{j}$ has magnitude $\sqrt{1^2 + 1^2} = \sqrt{2}$, which is not 1. None of the options appear to be unit vectors. However, option (d) $\frac{\hat{i} + \hat{j}}{\sqrt{2}}$ would be a unit vector. Based on the given answer key.
```