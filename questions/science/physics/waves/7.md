---
choices:
  - $\frac{v(t_1 - t_2)}{2}$
  - $\frac{v(t_1 t_2)}{2(t_1 + t_2)}$
  - $v(t_1 + t_2)$
answers:
  - $\frac{v(t_1 + t_2)}{2}$
---

**A person standing between two parallel hills fires a gun and hears the first echo after t₁ sec and the second echo after t₂ sec. The distance between the two hills is**

```markdown
Distance to first hill = vt₁/2. Distance to second hill = vt₂/2. Total distance between hills = vt₁/2 + vt₂/2 = v(t₁ + t₂)/2.
```